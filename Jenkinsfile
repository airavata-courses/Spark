pipeline {
    agent any

    stages {
        stage('Build React App') {
            steps {
                dir('findyournextmovie'){
                   sh 'npm install'
                }
            }
        }
    }
}
